#include "main.h"
#include <stdio.h>

/**
 * int_abs - entry point
 *
 * @c: parameter with type int
 *
 * Return: Always 0 (Success)
 */

int int_abs(int)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
	return (c * -1);
}	
